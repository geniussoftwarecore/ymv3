# دليل البدء السريع - نظام إدارة ورش يمن الهجين

**تاريخ**: نوفمبر 2024

**إعداد**: Manus AI

---

## للمستخدمين المتعجلين 🚀

هذا الدليل مخصص لمن يريد تشغيل المشروع بأسرع طريقة ممكنة.

### المتطلبات الأساسية

تأكد من تثبيت هذه الأدوات أولاً:

- [Docker Desktop](https://www.docker.com/products/docker-desktop/) (يتضمن Docker Compose)
- [Git for Windows](https://git-scm.com/download/win)
- [Flutter SDK](https://docs.flutter.dev/get-started/install/windows) (اختياري للواجهة الأمامية)

### خطوات التشغيل السريع

#### 1. تحميل المشروع
```cmd
git clone <repository-url>
cd yaman_workshop_system
```

#### 2. تشغيل السكريبت التلقائي (الطريقة الأسهل)
```cmd
# انقر بالزر الأيمن على setup-windows-complete.bat
# اختر "Run as administrator"
```

#### 3. التشغيل اليدوي (إذا فشل السكريبت)
```cmd
# إعداد Backend
cd backend
copy .env.example .env
docker-compose up --build -d

# إعداد Frontend (اختياري)
cd ..\frontend\yaman_hybrid_flutter_app
flutter pub get
flutter run -d chrome
```

### الوصول للنظام

بعد التشغيل الناجح:

- **الواجهة الأمامية**: http://localhost:5173 (إذا تم تشغيل Flutter)
- **API Gateway**: http://localhost
- **وثائق API**: http://localhost:8001/docs

### بيانات الدخول

- **البريد الإلكتروني**: `admin@yaman-workshop.com`
- **كلمة المرور**: `admin123`

### أوامر مفيدة

```cmd
# مراجعة حالة الخدمات
docker-compose ps

# مراجعة السجلات
docker-compose logs

# إيقاف الخدمات
docker-compose down

# إعادة تشغيل خدمة معينة
docker-compose restart user_management
```

### المشاكل الشائعة

| المشكلة | الحل السريع |
|---|---|
| منفذ 80 مستخدم | `netstat -ano \| findstr :80` ثم أوقف العملية |
| Docker لا يعمل | أعد تشغيل Docker Desktop |
| خطأ في بناء الصور | `docker-compose build --no-cache` |
| Flutter لا يعمل | `flutter doctor` للتحقق من المشاكل |

### للحصول على المساعدة

راجع **الدليل الشامل** (`windows_deployment_guide.md`) للحصول على شرح مفصل لكل خطوة.

---

**نصيحة**: احفظ هذا الدليل كمرجع سريع للمرات القادمة!
